# ============================================================
# Docker 构建上下文过滤
# ------------------------------------------------------------
# 目的：构建镜像时只把「真的要用到」的文件传给 Docker 守护进程。
# node_modules 动辄几百 MB，传上去纯属浪费带宽和时间。
# ============================================================

.git
.gitignore
.idea
.vscode

# 依赖与缓存（服务器上不需要，镜像里由容器自带）
**/node_modules
**/.vite

# 数据库导出（由 volume 挂载给 mysql，不走构建上下文）
deploy/seed

# 与运行无关的文档
docs
README.md
sql

# 日志
*.log
logs
