# ============================================================
# 部署配置模板
# ------------------------------------------------------------
# 用法：复制本文件为 .env 后按需修改。
#   cp .env.example .env
# .env 已被 .gitignore 排除，不会进版本库，也不会被打包进镜像。
# 三个值都必须改，deploy.sh 会拒绝带着占位符启动。
# ============================================================

# ------------------------------------------------------------
# 对外访问端口
#   80   → 用 http://你的公网IP 直接访问（推荐）
#   8080 → 用 http://你的公网IP:8080 访问（80 被占用时改这个）
# ------------------------------------------------------------
HTTP_PORT=80

# ------------------------------------------------------------
# 数据库 root 密码
# 【必须修改】不要用 root / 123456 / password 这类弱口令。
# 注意：该密码仅在「首次启动、数据卷为空」时写入 MySQL。
#      启动之后再改这里不会生效，需删除数据卷重建（见部署指南「忘记密码」）。
# ------------------------------------------------------------
DB_PASSWORD=请改成你自己的强密码

# ------------------------------------------------------------
# JWT 签名密钥
# 【必须修改】长度至少 32 字节（HS256 要求）。
# 为什么必须改：代码里带了一个公开的默认值。若不覆盖，等于把签名密钥
# 公之于众，任何人都能自行签发令牌、冒充任意用户登录。
# 生成方法（服务器上直接执行）：
#   openssl rand -base64 48
# ------------------------------------------------------------
JWT_SECRET=请粘贴一条随机字符串
